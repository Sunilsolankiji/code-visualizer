"use client";

const Header = () => {
  return (
    <header className="h-16 w-full bg-white shadow-md px-6 flex items-center fixed top-0 left-64 z-10">
      <h1 className="text-xl font-semibold">Dashboard</h1>
    </header>
  );
};

export default Header;
